# 永曜雲端科技有限公司

此專案由 `package.json` 指定為 Vite + Vue 3 + Quasar + Pinia 專案。

快速開始：

```bash
# 安裝相依
npm install

# 啟動開發伺服器
npm run dev

# 建置
npm run build

# 本機預覽（build 後）
npm run preview
```

說明：專案使用 `vite` 開發伺服器、`quasar` UI、`pinia` 狀態管理，並包含最小的檔案骨架（`src/main.ts`、`src/App.vue`、`src/router`）。
